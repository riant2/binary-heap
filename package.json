{
	"name": "@rickard.antonsson/binary-heap",
	"version": "1.2.0",
	"description": "Typescript implementation of a binary heap",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"build/src"
	],
	"license": "Apache-2.0",
	"keywords": [],
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"check": "gts check",
		"clean": "gts clean",
		"compile": "tsc -p .",
		"fix": "gts fix",
		"prepare": "npm.cmd run compile",
		"pretest": "npm.cmd run compile",
		"posttest": "npm.cmd run check",
		"start:dev": "nodemon",
		"start:debug": "nodemon --config nodemon-debug.json",
		"profile": "node --prof -r ts-node/register src/main.ts"
	},
	"devDependencies": {
		"@types/node": "^10.0.3",
		"gts": "^1.1.0",
		"ts-node": "^8.3.0",
		"tsconfig-paths": "^3.8.0",
		"typescript": "~3.5.0"
	},
	"dependencies": {},
	"repository": {
		"type": "git",
		"url": "binary-heap"
	},
	"author": "Rickard Antonsson"
}
